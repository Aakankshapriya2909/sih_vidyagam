# VidyaGam - Technical Architecture

## 🏗️ System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT (Browser)                         │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │  index.html  │  │   CSS Files  │  │  JS Modules  │         │
│  │              │  │              │  │              │         │
│  │  - Home      │  │  - main.css  │  │  - app.js    │         │
│  │  - Auth      │  │  - header    │  │  - Header    │         │
│  │  - Courses   │  │  - auth      │  │  - Auth      │         │
│  │  - Classes   │  │  - courses   │  │  - Navigator │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Game Pages (Standalone)                      │  │
│  │  - mathematics.html  - gk.html                           │  │
│  │  - HEL.html         - Lights.html                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ HTTP/HTTPS
                         │
┌────────────────────────▼────────────────────────────────────────┐
│                    EXPRESS.JS SERVER                             │
│                     (server.js - Port 3000)                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Route Handlers                        │   │
│  │                                                          │   │
│  │  GET /                    → index.html                   │   │
│  │  GET /mathematics         → mathematics.html             │   │
│  │  GET /gk                  → gk.html                      │   │
│  │  GET /physics-eye         → HEL.html                     │   │
│  │  GET /physics-light       → Lights.html                  │   │
│  │                                                          │   │
│  │  GET /api/courses         → courses.json                 │   │
│  │  GET /api/courses/:class  → filtered courses             │   │
│  │  GET /api/quiz/:subject   → quiz JSON                    │   │
│  │  POST /api/progress       → save progress                │   │
│  │                                                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              Static File Middleware                      │   │
│  │              express.static('public')                    │   │
│  └─────────────────────────────────────────────────────────┘   │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ File System Access
                         │
┌────────────────────────▼────────────────────────────────────────┐
│                      DATA LAYER                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────┐  ┌──────────────────┐                    │
│  │  Configuration   │  │   Course Data    │                    │
│  │                  │  │                  │                    │
│  │  app.config.json │  │  courses.json    │                    │
│  │                  │  │                  │                    │
│  │  - Languages     │  │  - Class 6-12    │                    │
│  │  - Classes       │  │  - Subjects      │                    │
│  │  - Subjects      │  │  - Topics        │                    │
│  │  - Features      │  │                  │                    │
│  └──────────────────┘  └──────────────────┘                    │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    Quiz Data (JSON)                       │  │
│  │                                                          │  │
│  │  quizzes/                                                │  │
│  │  ├── math.json        - Math questions                   │  │
│  │  ├── gk.json          - GK questions                     │  │
│  │  └── physics.json     - Physics questions                │  │
│  │                                                          │  │
│  │  Format: { questions: [ {id, question, options, ...} ] }│  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                Static Assets                              │  │
│  │                                                          │  │
│  │  assets/                                                 │  │
│  │  ├── Images (.jpg, .png)                                │  │
│  │  └── Videos (.mp4)                                       │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

### User Authentication Flow
```
User → Login Button → Auth Modal → Form Submit
  ↓
Validate Input → Store in LocalStorage → Update UI
  ↓
Show Profile → Access Restricted Content
```

### Course Selection Flow
```
Home Page → Click "Game-Based Learning"
  ↓
Class Selection (6-12)
  ↓
Select Class → Load Courses (API Call)
  ↓
Show Subjects: Math, Science, GK
  ↓
Select Subject → Navigate to Game/Quiz
```

### Quiz Flow
```
Select Quiz → API: GET /api/quiz/:subject
  ↓
Load Questions → Display Question
  ↓
User Answers → Check Correctness
  ↓
Update Score → Next Question
  ↓
All Done → Show Results
  ↓
Save Progress → API: POST /api/progress
```

## 🧩 Component Architecture

### Frontend Components

```
┌─────────────────────────────────────────┐
│         App.js (Main Controller)        │
│                                         │
│  - Initialize all components            │
│  - Setup event listeners                │
│  - Handle routing                       │
└────────────┬────────────────────────────┘
             │
    ┌────────┴─────────┬──────────────┬────────────┐
    │                  │              │            │
┌───▼────┐  ┌─────────▼─────┐  ┌────▼──────┐  ┌─▼────────┐
│ Header │  │ AuthComponent │  │  Course   │  │   Quiz   │
│        │  │               │  │ Navigator │  │Component │
│ - Nav  │  │ - Login Form  │  │           │  │          │
│ - Lang │  │ - Signup Form │  │ - Classes │  │ - Render │
│ - Auth │  │ - Validation  │  │ - Courses │  │ - Score  │
└────────┘  └───────────────┘  └───────────┘  └──────────┘
```

### Utility Modules

```
┌─────────────────────────────────────────┐
│           Utility Functions             │
├─────────────────────────────────────────┤
│                                         │
│  API.js                                 │
│  ├── get()      - Fetch data            │
│  ├── post()     - Send data             │
│  └── Endpoints                          │
│                                         │
│  Storage.js                             │
│  ├── getUser()  - Retrieve user         │
│  ├── setUser()  - Save user             │
│  └── Progress tracking                  │
│                                         │
│  Helpers.js                             │
│  ├── formatDate()                       │
│  ├── validate()                         │
│  └── General utilities                  │
└─────────────────────────────────────────┘
```

## 📡 API Structure

```
/api
├── /courses
│   ├── GET /            - All courses
│   └── GET /:class      - Courses by class
│
├── /quiz
│   └── GET /:subject    - Quiz questions
│
└── /progress
    └── POST /           - Save progress
```

### Request/Response Examples

**GET /api/courses/6**
```json
Response:
[
  {
    "id": "math_6",
    "class": 6,
    "subject": "math",
    "name": "Mathematics",
    "topics": ["Whole Numbers", "Fractions"]
  }
]
```

**GET /api/quiz/math**
```json
Response:
{
  "subject": "Mathematics",
  "questions": [
    {
      "id": 1,
      "question": "What is 2+2?",
      "options": ["3", "4", "5", "6"],
      "correct": 1
    }
  ]
}
```

## 🔐 Security Architecture

```
┌─────────────────────────────────────────┐
│            Security Layers              │
├─────────────────────────────────────────┤
│                                         │
│  Frontend                               │
│  ├── Input Validation                   │
│  ├── XSS Prevention                     │
│  └── CORS Headers                       │
│                                         │
│  Server                                 │
│  ├── Body Parser (JSON)                 │
│  ├── Static File Serving               │
│  └── Error Handling                     │
│                                         │
│  Data                                   │
│  ├── Read-only JSON                     │
│  └── LocalStorage (client-side)         │
└─────────────────────────────────────────┘
```

## 📦 Technology Stack

```
┌─────────────────────────────────────────┐
│           Technology Stack              │
├─────────────────────────────────────────┤
│                                         │
│  Backend                                │
│  ├── Node.js (Runtime)                  │
│  ├── Express.js (Web Framework)         │
│  └── body-parser, cors, dotenv          │
│                                         │
│  Frontend                               │
│  ├── HTML5                              │
│  ├── CSS3 (Custom + Responsive)         │
│  ├── JavaScript ES6+ (Vanilla)          │
│  └── Font Awesome (Icons)               │
│                                         │
│  Data                                   │
│  ├── JSON Files                         │
│  └── LocalStorage                       │
│                                         │
│  Development                            │
│  ├── nodemon (Auto-reload)              │
│  └── npm (Package Manager)              │
└─────────────────────────────────────────┘
```

## 🚀 Deployment Architecture

```
Development:
┌──────────┐
│ Localhost│
│ :3000    │
└──────────┘

Production (Recommended):
┌─────────────┐      ┌──────────┐      ┌────────────┐
│   Domain    │ ───► │  CDN     │ ───► │   Server   │
│ vidyagam.com│      │ (Static) │      │  (Node.js) │
└─────────────┘      └──────────┘      └────────────┘
                           │                   │
                           └───────┬───────────┘
                                   ▼
                            ┌──────────────┐
                            │   Database   │
                            │  (Optional)  │
                            └──────────────┘
```

## 📊 Performance Considerations

1. **Static Assets**: Cached by browser
2. **API Responses**: Lightweight JSON
3. **Component Loading**: On-demand
4. **LocalStorage**: Fast client-side storage
5. **No External Dependencies**: Fast page load

## 🔄 State Management

```
Application State Flow:

LocalStorage ←──────────────────┐
     ↓                           │
User Session ──→ Components ──→ UI
     ↓                           │
API Calls ──→ Data ──────────────┘
```

---

**This architecture ensures:**
- ✅ Modularity
- ✅ Scalability
- ✅ Maintainability
- ✅ Performance
- ✅ Security
